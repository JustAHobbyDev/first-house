# Pre-birth bootstrap implementation plan

1. Inspect and preserve the existing handoff and unrelated genome-harness analysis.
2. Establish strict TypeScript, Node's SQLite/event storage, immutable artifacts,
   runtime-validated domain commands, and deterministic projections.
3. Implement versioned packs and deterministic context compilation with pinned
   provenance, required-pack failure semantics, and immutable manifests.
4. Add the fake adapter and one artifact-writing capability with recorded policy
   decisions and recovery after an interrupted effect.
5. Add an offline disposable demonstration, migration/verification/replay commands,
   meaningful tests, and architectural and pre-birth documentation.
6. Run formatting, type checking, tests, and the demonstration; inspect generated
   state and the final file set. Do not commit, push, or deploy.

All identities and stores created by this milestone are disposable development
fixtures. No canonical House is constituted.
